# Чеклист готовности к созданию первого инструмента

## ✅ Готово

### Компоненты
- [x] `CalculatorWidget` - создан и работает
- [x] `Breadcrumbs` - создан и работает
- [x] Все импорты корректны
- [x] Проект успешно компилируется

### Структура страницы
- [x] Desktop layout с float (виджет слева, реклама справа, текст обтекает)
- [x] Mobile layout с инъекцией баннеров между h2
- [x] Breadcrumbs отображаются корректно
- [x] H1 берется из `tool_i18n.h1` или `tool_i18n.title`

### Переводы
- [x] Все переводы для виджета добавлены в `translations.ts`
- [x] CSV файл обновлен со всеми переводами
- [x] Поддерживаемые языки: EN, RU, DE, ES, FR, IT, PL, LV

### База данных
- [x] Функция `getToolData` работает
- [x] Функция `getPageByCode` для страницы виджета работает
- [x] Структура БД поддерживает все необходимые поля

### Функциональность
- [x] Калькулятор работает с JSON engine
- [x] Share links поддерживаются
- [x] Копирование результата работает
- [x] Кнопки Clear и Calculate работают

## ⚠️ Требует внимания

### Опциональные страницы
- [ ] Страница `/contact` - ссылка "Suggest an improvement" ведет на `/${lang}/contact`
  - **Решение**: Создать страницу contact или сделать ссылку условной
- [ ] Страница с кодом `id105` для виджета - ссылка показывается только если страница существует
  - **Решение**: Создать страницу или оставить опциональной (уже реализовано)

### Минимальные требования для инструмента

Для создания инструмента необходимо:

1. **Обязательные поля в `tool_i18n`:**
   - `slug` - уникальный slug инструмента
   - `title` - название инструмента
   - `lang` - язык

2. **Рекомендуемые поля:**
   - `h1` - заголовок для виджета (если не указан, используется `title`)
   - `intro_text` - вступительный текст
   - `inputs_json` - переводы для полей ввода (JSON)

3. **Для работы калькулятора:**
   - `tool.config.config_json` - конфигурация JSON engine
   - Должен содержать:
     - `inputs` - массив полей ввода
     - `outputs` - массив полей вывода
     - `formula` - функция расчета

4. **Для контента (опционально):**
   - `content_blocks_json` - произвольный структурированный контент с заголовками h2 (массив блоков)
   - `examples_json` - примеры использования
   - `formula_md` - формула в Markdown
   - `assumptions_md` - предположения в Markdown
   - `faq_json` - FAQ в JSON формате

   **Структура content_blocks_json:**
   ```json
   [
     {
       "type": "h2",
       "content": "Заголовок секции",
       "id": "section-id" // опционально
     },
     {
       "type": "paragraph",
       "content": "Текст параграфа"
     },
     {
       "type": "html",
       "content": "<p>HTML контент</p>"
     },
     {
       "type": "section",
       "content": "<div>HTML секция</div>",
       "id": "section-id" // опционально
     }
   ]
   ```
   
   **Важно:** Баннеры в мобильной версии автоматически вставляются перед каждым h2 (начиная со второго):
   - Перед 2-м h2 → Баннер 2
   - Перед 3-м h2 → Баннер 3
   - Перед 4-м h2 → Баннер 4

5. **Связь с категорией:**
   - Инструмент должен быть связан с категорией через `ToolCategory`

## 📝 Пример структуры данных

```typescript
{
  slug: "numbers-to-words",
  type: "calculator",
  engine: "json",
  i18n: {
    en: {
      title: "Numbers to Words Converter",
      h1: "Numbers to Words Converter",
      intro_text: "Convert numbers to words...",
      inputs_json: {
        inputs: {
          number: {
            label: "Convert this Number:",
            placeholder: "Enter number"
          }
        }
      }
    }
  },
  config: {
    inputs: [
      { key: "number", type: "number", default: 0 }
    ],
    outputs: [
      { key: "result", precision: 0 }
    ],
    formula: "function calculate(inputs) { return { result: convertToWords(inputs.number) } }"
  }
}
```

## 🚀 Готово к использованию

Все основные компоненты готовы. Можно начинать создавать первый инструмент!

